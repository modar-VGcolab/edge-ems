# v1 lab config — OT VLAN only. TLS + credentials are a release gate before
# any site with WAN exposure (architecture §8, plan task 32).
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
